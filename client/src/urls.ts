/**
 * Endereços que saem deste site.
 *
 * Fora do dicionário de propósito: URL não é texto traduzível, e o dicionário
 * duplica tudo em `pt` e `en` — o endereço da plataforma passaria a existir em
 * dois lugares que precisariam mudar juntos. (Existe um bloco `links` lá com
 * três URLs por esse caminho; nenhum componente o lê.)
 */

/** A plataforma do programa. A landing apresenta; entrar acontece lá. */
export const PLATAFORMA = "https://app.momento.sh";
